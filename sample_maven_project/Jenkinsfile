pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                script {
                    if (isUnix()) {
                        sh 'echo "Building in Unix"'
                    } else {
                        powershell 'echo "Building in Windows"'
                    }
                }
            }
        }
    }
}
